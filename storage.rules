rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Permite leer y escribir solo si el usuario ha iniciado sesión
      allow read, write: if request.auth != null;
    }
  }
}
